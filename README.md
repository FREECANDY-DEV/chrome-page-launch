# chrome-page-launch

A local page that opens a new Chrome from the terminal every time you visit or refresh it. The terminal stays on screen.

## Run

```bash
python3 server.py
```

Then open http://127.0.0.1:8765/

Needs `google-chrome`, `xfce4-terminal`, and a display. Each visit launches a separate Chrome profile.
